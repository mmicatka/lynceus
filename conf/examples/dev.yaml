---
# conf/examples/dev.yaml

# uri_list: assets/candidate_uris.blackhole.txt
candidates_local_path: "/app/lynceus/work/assets/candidates/*.smi.gz"

outdir: results/dev

filter:
  config: conf/examples/physiochemical_filter.yaml
  batch_size: 50000

surrogate_model:
  training:
    seed: 1000
    num_samples: 1000

target:
  ensemble:
    ensemble_id: "T4_lysozyme_L99_static"
    type: "static"
    components:
      - pdb_id: "1L83"
        weight: 1.0

    capabilities_required:
      - "standalone_cif"
