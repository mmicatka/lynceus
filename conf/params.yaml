---
# conf/params.yaml

candidates:
  uri_list: assets/candidates.sample.txt
  filter:
    config: "/app/lynceus/work/conf/examples/physiochemical_filter.yaml"
    batch_size: 50000

surrogate_model:
  training:
    seed: 1000
    num_samples: 10000

target:
  ensemble:
    ensemble_id: "T4_lysozyme_L99_static"
    type: "static"
    components:
      - pdb_id: "1L83"
        weight: 1.0

    capabilities_required:
      - "standalone_cif"
