# Default parameters for the Lynceus pipeline.
# Override with: nextflow run main.nf -params-file my_params.yaml
# or: nextflow run main.nf --uri_list path/to/uris.txt

uri_list: assets/candidate_uris.txt

outdir: results
