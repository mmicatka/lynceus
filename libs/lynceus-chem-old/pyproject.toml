# libs/lynceus-chem/pyproject.toml

[project]
name = "lynceus-chem-old"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.13.4"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
sources = ["src"]
packages = ["src/chem"]
