---
repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff
        entry: uv run ruff check .
        language: system
        types: [python]
      - id: yamllint
        name: yamllint
        entry: uv run yamllint .
        language: system
        types: [yaml]
