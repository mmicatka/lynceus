---
repos:
  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.22.1
    hooks:
      - id: markdownlint-cli2

  - repo: local
    hooks:
      - id: yamllint
        name: yamllint
        entry: uvx yamllint .
        language: system
        types: [yaml]
